export default class MenuItem {
  /**
   * MenuItem
   * @param {String} title
   * @param {String} link
   * @param {String} icon
   */
  constructor(title, link, icon) {
    this.title = title
    this.link = link
    this.icon = icon
  }
}
